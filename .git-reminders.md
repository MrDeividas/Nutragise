# Git Commit Reminders

## ⚠️ NEVER COMMIT THESE TO GIT:

1. **Secret Keys & API Keys**
   - Stripe secret keys (sk_test_*, sk_live_*)
   - Supabase service role keys
   - API keys (DeepSeek, etc.)
   - Webhook secrets

2. **Environment Files**
   - `.env` files (already in .gitignore)
   - `.env.local`
   - `.env.production`
   - Any file containing actual secret values

3. **Sensitive Configuration**
   - Actual API keys in documentation files
   - Real credentials in code comments
   - Database connection strings with passwords

## ✅ ALWAYS USE:
- Placeholders in documentation: `sk_test_YOUR_STRIPE_SECRET_KEY_HERE`
- Environment variables for secrets
- `.gitignore` to exclude sensitive files

## 📝 Before Committing:
- Check for hardcoded secrets: `grep -r "sk_test\|sk_live" --exclude-dir=node_modules`
- Verify `.env` is in `.gitignore`
- Review documentation files for actual keys

